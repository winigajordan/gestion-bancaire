{% extends 'base.admin.html.twig' %}
{% block content %}
    <div class="container">
        <div class="card">
            <div class="card-header">
                Interface Admin
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-auto"> <a href="{{ path("admin_load_type_compte") }}" class="btn btn-outline-success"> Gestion des types de compte</a> </div>
                    <div class="col-auto"> <a href="{{ path('admin_load_type_operation') }}" class="btn btn-outline-success"> Gestion des types d'operation</a> </div>
                    <div class="col-auto"> <a href="#" class="btn btn-outline-success"> Afficher les statistiques </a> </div>
                    <div class="col-auto"> <a href="#" class="btn btn-outline-success"> Gestion des types de compte</a> </div>
                </div>
            </div>
        </div>
    </div>

    {% block action %} {% endblock %}
{% endblock %}